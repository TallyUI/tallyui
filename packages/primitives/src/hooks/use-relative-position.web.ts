// Web stub — on web we use Radix positioning, so this returns an empty object.

import type { LayoutPosition } from './use-relative-position';

export function useRelativePosition(_args: Record<string, unknown>) {
  return {};
}

export type { LayoutPosition };
