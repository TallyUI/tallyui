---
"@tallyui/database": minor
---

`createTallyDatabase` takes `multiInstance` (default false) to share the database between browser tabs. When enabled, it also enables RxDB local documents for shared outbox state and flush requests.
