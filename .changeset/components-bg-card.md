---
"@tallyui/components": patch
---

Order cards, receipt preview, register summary, settings groups, connector status, variant picker, cash-tendered input, cart note and customer form used the undefined `bg-surface` class and rendered transparent; they now use `bg-card`.
