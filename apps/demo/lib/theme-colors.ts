/**
 * The theme's primary colour for demo styles that cannot use classes
 * (StyleSheet objects, `color` props). Must equal --color-primary in
 * packages/theme/src/tokens.css; theme-classes.test.ts checks it.
 */
export const PRIMARY = '#5b5ef0';

/**
 * The theme's muted foreground colour for demo styles that cannot use classes
 * (StyleSheet objects, `color` props). Must equal --color-muted-foreground in
 * packages/theme/src/tokens.css; theme-classes.test.ts checks it.
 */
export const MUTED_FOREGROUND = '#656c79';
